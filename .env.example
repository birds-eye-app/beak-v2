# Copied to .env (gitignored) for local builds. CI gets the same value from a repository secret.
# Mapbox PUBLIC token (pk.…), URL-restricted to dtmeadows.me + localhost in the Mapbox dashboard.
MAPBOX_TOKEN=
